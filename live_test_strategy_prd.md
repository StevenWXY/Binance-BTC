# BTCUSDT 实盘测试策略 PRD

## 1. 文档信息

- 文档名称：`BTCUSDT 实盘测试策略 PRD`
- 适用阶段：第一阶段影子盘与小仓实盘测试
- 策略范围：基于 `main` 与 `xuyujian` 两条研究路线收敛出的实盘测试方案
- 参考文档：[main_vs_xuyujian_fair_comparison.md](file:///Users/apple/Documents/Binance-BTC/Binance-BTC-wzy/main_vs_xuyujian_fair_comparison.md)

## 2. 背景与目标

当前工作区内有两条可用研究路线：

- `main`：V4 家族较完整，执行层、保护退出、资金分配与压力测试更成熟
- `xuyujian`：V6/V7/V7.1 在状态机和做空表达上更强，但复杂度更高

本 PRD 的目标不是直接挑选“历史收益最高”的版本，而是设计一套**适合实盘测试**的方案，满足以下要求：

1. 先上线一个可解释、可复核、可控回撤的主策略
2. 同时保留一条更激进的影子策略，验证是否值得进入下一阶段
3. 让实盘测试优先暴露执行偏差、滑点偏差和风控缺口，而不是先暴露过大的方向性风险

## 3. 产品目标

### 3.1 核心目标

- 用最小真实资金完成 BTCUSDT 永续的第一阶段策略试运行
- 验证回测信号是否能稳定映射到真实下单与真实仓位
- 验证 `main` 路线是否足够稳健到可以扩仓
- 验证 `xuyujian` 路线在真实市场中是否存在稳定增益

### 3.2 非目标

- 不是第一阶段就追求最大收益
- 不是第一阶段就启用高强度对称做空
- 不是第一阶段就引入复杂多策略自适应切换
- 不是第一阶段就以实盘结果重新过度调参

## 4. 目标用户与使用场景

### 4.1 目标用户

- 当前研究者本人
- 后续需要复盘、排障、调参的协作者

### 4.2 使用场景

- 每 4 小时完成一轮信号计算
- 根据信号、风险层与执行规则生成目标仓位
- 执行层决定是挂单、吃单、减仓还是观望
- 每日/每周对照回测信号、真实成交、真实回撤和真实资金费

## 5. 策略总方案

## 5.1 第一阶段推荐组合

### 主策略

- 来源：`main`
- 方向层：`V4.1`
- 执行保护层：吸收 `V4.3` 的保护退出逻辑

### 影子策略

- 来源：`xuyujian`
- 策略：`V7.1`
- 运行方式：先影子盘记录；必要时再用极小仓位试单

### 为什么这样组合

- `V4.1` 足够简单，便于实盘验证信号与执行一致性
- `V4.3` 的快速下跌保护和显式退出更适合真实交易
- `V7.1` 具有更强的收益弹性，但更适合先做旁路验证

## 5.2 第一阶段策略定位

可以把这套设计理解成：

- `main` 负责“稳”
- `xuyujian` 负责“看是否值得更进攻”

也就是：

- 主仓位用稳健长多框架
- 影子仓位用更复杂的状态机做旁路验证

## 6. 主策略设计

## 6.1 信号框架

主策略采用 `main` 的 `V4.1` 方向框架：

- 趋势环境：做趋势多头
- 非趋势环境：仅在超跌条件满足时做小仓位反弹
- 不启用主策略级别的常规趋势做空

## 6.2 买入逻辑

### 趋势买入

当以下条件成立时，在下一交易边界建立或维持多头：

- 4 小时趋势状态成立
- 均线距离足够，不是弱趋势
- 风险层未强制大幅缩仓

### 反弹买入

仅在非趋势环境下允许：

- RSI 进入超跌区
- 收盘价接近或跌破布林带下轨
- 仓位低于趋势单

## 6.3 卖出逻辑

### 常规退出

以下任一情况发生时，降低目标仓位或退出：

- 趋势条件失效
- 反弹完成
- 风险层切换到收缩状态

### 保护退出

吸收 `V4.3` 的执行保护逻辑：

- ATR 止损
- ATR 跟踪止损
- 快速下跌保护退出
- 必要时使用 taker 保护平仓，不为省手续费延迟退出

## 6.4 仓位与杠杆

第一阶段主策略要求：

- 继续使用波动率目标仓位
- 保持代码级硬杠杆上限不变
- 第一阶段实盘风控上限建议低于回测理论上限

建议的实盘测试上限：

- 主策略目标杠杆上限：`2.0x - 3.0x`
- 单次风险暴露：显著低于完整回测版本

也就是说，第一阶段要先验证“策略会不会稳”，而不是验证“6.5 倍能不能跑起来”。

## 7. 影子策略设计

## 7.1 影子策略目标

影子策略采用 `xuyujian` 的 `V7.1`，目标不是立刻赚钱，而是验证：

1. 三态状态机是否在真实市场中比 V4.1 更有效
2. 受限做空是否真的能带来稳定增益
3. 复杂策略在真实成交环境下是否还能保持回测优势

## 7.2 运行方式

第一阶段推荐两种方式二选一：

- 纯影子盘：只记录信号、理论成交和理论仓位
- 极小仓位试单：风险预算不超过主策略的 `10% - 20%`

## 7.3 影子策略限制

第一阶段必须加三条限制：

1. 对称做空仅保留受限版本，不放大 short scale
2. 不允许影子策略反向覆盖主策略的大部分净仓位
3. 不允许实盘阶段边跑边频繁改参数

## 8. 执行系统需求

## 8.1 下单频率

- 主频：4 小时边界
- 允许分钟级执行与保护退出

## 8.2 订单类型

第一阶段建议：

- 主策略开仓/调仓以保守成交为主
- 保护退出优先使用高确定性退出方式
- maker 逻辑可做，但不能牺牲退出速度

换句话说：

- 开仓可以考虑“更省手续费”
- 止损和平仓要优先“更确定退出”

## 8.3 成交与偏差记录

实盘系统必须记录：

- 目标仓位
- 实际下单价格
- 实际成交价格
- 未成交原因
- 延迟成交带来的偏差

## 9. 风控需求

## 9.1 必须上线的风控

第一阶段必须具备：

- 目标杠杆上限
- 最大单边仓位上限
- 快速下跌保护
- ATR 保护退出
- 日内/周内最大回撤阈值
- 资金费暴露监控

## 9.2 账户级风控

建议设置三档账户级风控：

1. 轻度回撤：只缩小新开仓目标
2. 中度回撤：降低整体目标仓位
3. 重度回撤：只允许减仓，不允许加仓

这部分可以借鉴 `V7.1` 的执行层回撤降仓思想，但建议先用于**账户级保护**，而不是让信号层变得更复杂。

## 10. 数据与监控需求

## 10.1 数据需求

实盘系统至少需要持续记录：

- 4h 信号输入数据
- 1m 成交数据
- 标记价格
- 资金费率
- 实盘订单与成交回报

## 10.2 监控面板

建议第一阶段就做出四类监控：

1. 信号监控
2. 持仓监控
3. 成本监控
4. 风险监控

关键字段包括：

- 当前信号方向
- 当前目标杠杆
- 当前实际杠杆
- 成交偏差
- 当日资金费
- 当前回撤
- 可用保证金缓冲

## 11. 上线节奏

## 11.1 阶段 1：影子盘

周期建议：`2-4 周`

目标：

- 验证信号生成稳定性
- 验证本地回测与线上运行是否一致
- 记录主策略和影子策略的实盘偏差

通过标准：

- 无严重信号漂移
- 无连续多次异常下单
- 成交偏差可解释

## 11.2 阶段 2：小仓试单

周期建议：`4-8 周`

目标：

- 主策略进入小仓真实成交
- 影子策略继续旁路，或极小仓位试单

建议配置：

- 主策略：总风险预算 `5% - 10%`
- 影子策略：主策略风险预算的 `10% - 20%`

## 11.3 阶段 3：双策略对照评估

重点对比：

- 主策略与影子策略的真实滑点差异
- 回测收益与真实收益偏差
- 成本偏差
- 回撤偏差
- 做空时段的表现稳定性

## 11.4 阶段 4：扩仓或淘汰

只有在以下条件满足时，才考虑让影子策略进入更高权重：

- 连续多个评估周期表现稳定
- 做空没有显著放大账户级回撤
- 真实成交偏差没有破坏优势
- 复盘后没有发现系统性实现错误

## 12. 验收指标

## 12.1 主策略验收指标

第一阶段重点看：

- 最大回撤是否低于预设阈值
- 信号是否稳定
- 成交是否稳定
- 风险退出是否可靠

不是第一阶段重点的指标：

- 是否跑赢所有历史高收益候选

## 12.2 影子策略验收指标

重点看：

- 是否持续优于主策略的影子收益
- 是否在真实市场中仍能保持更高信息比率
- 是否在做空时段真正提供增益，而不是只增加波动

## 13. 开放问题

当前仍有几个需要后续明确的问题：

1. 主策略第一阶段是否启用 maker 开仓，还是全部先用更确定的成交方式
2. `V4.2` 的 neutral sleeve 是否进入第二阶段测试，还是先只做方向层
3. 影子策略是否在第一阶段就允许小比例真实做空
4. 是否需要单独为实盘构建“研究参数”和“上线参数”两套配置

## 14. 最终决策建议

本 PRD 建议的第一阶段实盘测试策略是：

- **主策略**：`main` 的 `V4.1`
- **保护层**：吸收 `main` 的 `V4.3` 保护退出
- **影子策略**：`xuyujian` 的 `V7.1`

这套方案的优先级是：

1. 先验证策略与执行闭环
2. 再验证高复杂度策略是否值得进入真实资金层
3. 先控制偏差和回撤，再讨论扩仓与收益上限

一句话概括：

> 第一阶段实盘测试，不选历史最猛的，而选最容易活着跑完第一轮验证的。 
