# V71 Bottom-Up Redesign Brief

## 1. 背景

当前 `wzy` 路线已经完成了以下几轮工作：

- 三路线同口径比较
- `v71_live` 小范围参数扫描
- `ref_plus_live / round2_best / final_candidate` 候选验证
- governor 恢复机制接入
- live-like 活跃度诊断
- 趋势判断质量诊断

现阶段暴露出的核心问题不是单一参数失配，而是更底层的结构性问题：

1. 不同年份市场结构差异明显，继续只围绕现有阈值微调，存在较强过拟合风险。
2. `2020` 对总利润贡献偏大，后续年份表现更平，说明策略有效性不能只靠全样本汇总指标判断。
3. `trend_up / trend_down / range` 虽然能提供方向框架，但还没有充分表达“这段行情是否值得交易”。
4. `trend_down` 与 `trend_up` 的质量并不对称，做空不能继续当作做多的镜像版本处理。

因此，下一轮不建议先继续做局部参数打磨，而建议先做一轮底层机制改造。

## 2. 改造目标

目标不是做一版更激进的参数，而是把系统从“方向分类器”升级为“可交易性筛选器 + 独立方向模块 + 分层风险执行”。

本轮底层机制改造应满足以下要求：

1. 降低跨年份失效的概率，优先提高结构稳健性。
2. 让收益来源不再过度依赖单一年份或少数极端趋势段。
3. 让趋势判断模块输出的不只是方向，还包括趋势延续性与可交易性。
4. 保留现有 live protection、maker 追求与账户级 governor 思路。

## 3. 现有结构的问题

### 3.1 方向判断强于交易判断

当前三态更接近“市场方向状态”，而不是“是否值得在这一段主动承担趋势仓位”。

这会导致两个问题：

- 方向可能大体正确，但行情延续性不足，进场后很容易变成假突破。
- `range -> trend` 的状态切换过于依赖固定阈值，容易被年份间结构差异放大。

### 3.2 做空逻辑过于镜像化

BTC 的上行与下行结构并不对称。上行更常见的是趋势延续和缓慢抬升，下行则更常见于：

- 快速风险释放
- 波动率陡升
- 反弹与二次下探交替

因此，`trend_down` 如果继续用 `trend_up` 的镜像门槛，很容易在反弹市或震荡下行市中频繁误判。

### 3.3 状态输出缺少连续强弱信息

当前状态更多是离散标签，缺少“证据强弱”的表达。这会让后续仓位、开仓权限和退出容忍度都过于离散。

## 4. 建议的新结构

### 4.1 第一层：Tradability Filter

先判断“当前市场是否适合趋势交易”，再判断方向。

建议新增一个独立的 `tradability_score`，由以下证据组成：

- 方向效率：如 Efficiency Ratio / Aroon spread / ADX persistence
- 波动质量：ATR 扩张是否伴随方向位移，而不是纯噪声放大
- 结构延续：突破后 1-2 根 bar 是否继续站稳，而不是立刻回落
- 交易成本适配：资金费、滑点、maker 等成本是否允许这段行情值得参与

建议把它划分为三档：

- `untradable`
- `tradable_weak`
- `tradable_strong`

只有在 `tradable_weak` 以上，才允许趋势模块真正发出开仓许可。

### 4.2 第二层：Direction Engine

在 Tradability Filter 通过后，再由方向模块输出：

- `bias_up`
- `bias_down`
- `neutral`

但这里不应直接等价于最终开仓方向，而应作为“方向偏置”。

建议保留“两段式”架构：

- 日线负责大方向偏置
- 4h 负责触发和失效确认

但将输出从单纯三态，升级成：

- `daily_bias`
- `trigger_direction`
- `trend_quality_score`

这样后续执行层拿到的是更完整的上下文，而不是只有一个硬标签。

### 4.3 第三层：Independent Long / Short Modules

做多与做空应拆成两个独立模块，而不是同一套规则镜像翻转。

建议：

- `long_continuation_module`
  - 偏向处理突破后延续、回踩再上、低资金费压力下的趋势推进
- `short_release_module`
  - 偏向处理风险释放、波动放大、反弹失败后的二次下破

其中做空模块应天然更保守：

- 默认更低预算
- 更强调波动率、加速度与确认长度
- 对日线偏空的依赖更强

### 4.4 第四层：Continuous Position Permission

把状态输出映射为连续权限，而不是简单的开/不开。

例如：

- `tradability_score`
- `trend_quality_score`
- `drawdown_governor_scale`
- `speed_mode_scale`

最终共同形成：

- 是否允许开仓
- 允许做多还是做空
- 允许的最大信号强度
- 保护退出应该有多敏感

这样能减少“刚切换到 trend 就满配尝试”的问题。

### 4.5 第五层：Execution & Risk 保持独立

现有执行保护层仍然有价值，不建议推翻：

- maker 优先
- micro path 因果成交
- ATR stop / trailing stop
- downside protection
- 账户级 governor
- recovery probe

但执行层应更多承担“把正确但不完美的趋势判断做得更稳”的角色，而不是替代趋势判断本身。

## 5. 下一轮验证口径

底层机制改造后，验证方式也应同步升级。

建议固定为以下四层：

1. `train_2020_2022`
2. `validation_2023_2024`
3. `holdout_2025_2026`
4. 全样本只做汇总展示，不作为唯一决策依据

除了 `Sharpe / Calmar / Max DD`，还应固定看：

- 年度 trade count / fill count
- 年度收益是否过度集中
- 前 10% 大盈利交易对总收益的贡献占比
- 多头和空头各自的年度表现
- `trend_quality_score` 与实际交易质量的对应关系

## 6. 本轮先不直接落地的三类收紧动作

以下三类动作是下一轮实现时的重要注意点，但不建议在机制未改造前直接作为主优化方向：

### 注意点 A：收紧 `trend_down` 触发

这是合理方向，但它应放在“独立空头模块”下落地，而不是简单提高一两个全局阈值。

否则容易出现：

- 空头机会进一步减少
- 只改善个别年份
- 把问题从“误判做空”变成“完全不做空”

### 注意点 B：降低 `range -> trend` 假突破

这是当前最重要的局部问题之一，但它应由 `tradability_score + trend_quality_score` 共同处理，而不只是继续机械加大 `breakout_buffer_atr`。

否则容易把：

- 假突破过滤掉
- 真突破也一起过滤掉

### 注意点 C：严格限制 `range` 状态直接开仓

这个方向也是对的，尤其当前报告里 `range` 入场质量偏差。

但建议把它设计成：

- `range` 默认不开新方向仓
- 只有在特定例外条件下允许极小试探

而不是简单“一刀切禁止”，否则会损失少数真正由震荡转趋势的起点段。

## 7. 建议的实现顺序

建议下一轮按以下顺序推进：

1. 先重构状态输出：加入 `tradability_score / trend_quality_score`
2. 再拆分 long / short 两套入场资格逻辑
3. 再定义仓位许可和执行层映射
4. 最后才做局部参数扫描

如果顺序反过来，参数会提前固化在旧结构上，过拟合风险仍然较高。

## 8. 当前结论

当前最重要的不是“再把某个阈值调小一点”，而是先把系统回答清楚这三个问题：

1. 这段行情是否适合做趋势？
2. 如果适合，方向偏置究竟是什么？
3. 这次信号的证据强弱是否值得承担真实仓位？

只要这三个问题回答得更清楚，后续再讨论：

- 收紧 `trend_down`
- 降低 `range -> trend` 假突破
- 限制 `range` 直接开仓

才会更稳，也更不容易陷入年份级过拟合。
