# V7.1-live 设计说明

## 1. 目标

这版设计的目标不是简单复制 `xuyujian` 的 `V7.1`，而是在 `Binance-BTC-wzy/` 中形成一个更适合实盘测试的版本：

- 保留 `V7/V7.1` 的趋势判断优势
- 引入 `main/wzy` 的保护退出与压力测试思路
- 加入账户级回撤治理
- 最终可以和 `main`、`xuyujian` 在同一执行口径下回测比较

## 2. 八项核心机制解释

### 2.1 三态市场分类：`range / trend_up / trend_down`

这一步不是直接决定下单，而是先给市场贴标签。

- `range`：市场更像震荡，没有明确趋势优势
- `trend_up`：市场更像上涨趋势，优先考虑趋势多头
- `trend_down`：市场更像下跌趋势，允许受限做空

它的价值是把“低吸逻辑”和“追趋势逻辑”分开，避免同一时刻既想抄底又想追涨。

### 2.2 breakout buffer

这是突破确认缓冲。

例如价格刚刚越过过去一段时间高点，系统不会立刻认定这是有效突破，而是要求它再多穿出去一小段距离。这里的距离按 ATR 表示。

作用：

- 减少假突破
- 减少震荡边缘来回切状态
- 提高趋势分类的纯度

### 2.3 日线 + 4h 多层趋势确认

这是“两段式趋势判断”的核心。

- 日线层：负责给出更慢的方向偏置，判断大环境偏多、偏空还是中性
- 4h 层：负责具体执行，判断这根 bar 是否够资格进入趋势状态

作用：

- 避免仅靠 4h 噪声就翻多翻空
- 让短周期信号服从长周期方向
- 把趋势判断做成“先定大方向，再找入场”

### 2.4 rapid / deceleration 仓位收缩逻辑

这套逻辑用来处理“趋势太快”和“趋势开始减速”两件事。

- `rapid`：价格跑得很快，说明趋势很强，但也可能开始拥挤
- `deceleration`：虽然还在涨或跌，但速度明显慢下来了

当趋势处在 rapid 状态又出现 deceleration 时，系统会缩仓。

作用：

- 防止在最亢奋的位置还维持满风险
- 提前把一部分利润从快速趋势里拿回来
- 减少趋势尾部反转的损伤

### 2.5 V4.3 的快速下跌保护

这是从 `main/wzy` 借来的。

它不是等完整趋势逻辑走坏，而是专门盯着“刚刚完成的 4h K 线里有没有异常快速下跌”。

一旦出现这种情况，就会：

- 立刻把多头目标降到更保守的位置
- 或者直接触发保护退出

作用：

- 专门处理“趋势判断还没来得及变坏，但市场已经在快速下杀”的场景
- 降低大阴线、连跌和闪崩对多头策略的伤害

### 2.6 显式 ATR 止损 / 跟踪止损

这一步不是靠市场状态决定，而是靠持仓后的价格行为决定。

- `stop_atr`：开仓后，如果价格朝反方向走太多，直接止损
- `trailing_stop_atr`：如果持仓已经盈利，就跟着价格更新保护线
- `take_profit_atr`：到达预设盈利距离后可以退出

作用：

- 把“趋势分类”和“仓位保护”拆开
- 趋势判断出错时更快退出
- 趋势走完时更容易保住利润

### 2.7 `stress.py` 的极端路径验证

这不是新信号，而是验证框架。

它会构造几类确定性的极端行情，例如：

- flash crash
- gap down / gap up
- funding spike
- liquidity crunch
- trend reversal

然后用同一个执行引擎去测：

- 会不会强平
- 止损是否真的触发
- 回撤是否明显失控

作用：

- 避免只在正常历史样本里看起来很好
- 逼策略在“最不舒服的路径”里暴露问题

### 2.8 更偏实盘的执行与退出思路

这一步指的是：

- 常规开仓和调仓仍然追求 maker
- 风险退出优先确定性，不因为省手续费而延迟
- 同时记录实际成交偏差、挂单超时、maker fill ratio

作用：

- 让回测结果更接近未来真实执行
- 把“信号正确”和“能不能成交”这两件事分开看

## 3. 六个参数的含义

### 3.1 `breakout_buffer`

代码里对应 `breakout_buffer_atr`。

含义：

- 价格突破过去高点/低点后，还要再走出多少个 ATR，才算有效突破

数值越大：

- 越保守
- 越能过滤假突破
- 但越容易晚进

### 3.2 `trend_confirm_bars`

含义：

- 4h 趋势信号要连续满足多少根 bar，才允许从 `range` 切到趋势状态

数值越大：

- 趋势确认更稳
- 误判更少
- 但反应更慢

### 3.3 `trend_exit_confirm_bars`

含义：

- 已经处于趋势状态后，要连续多少根 bar 显示趋势转弱，才退出趋势状态

数值越大：

- 更不容易被小回撤洗出去
- 但也更容易在趋势结束后退得慢

### 3.4 `trailing_stop_atr`

含义：

- 持仓盈利后，保护止损线和价格极值之间保持多少 ATR 的距离

数值越小：

- 保护更紧
- 回撤更小
- 但更容易被正常波动扫掉

数值越大：

- 更能吃完整趋势
- 但利润回吐会更多

### 3.5 `rapid_deceleration_scale`

含义：

- 当系统判定当前处在 rapid 趋势、但速度开始减弱时，仓位还保留多少比例

例如 `0.40` 表示：

- 原本趋势仓位只保留 40%

数值越小：

- 越强调“先锁利润”
- 趋势尾部回撤更小
- 但可能少吃后半段

### 3.6 `account_drawdown_governor`

代码里分解为一组账户级参数：

- `strategy_drawdown_level_1 / scale_1`
- `strategy_drawdown_level_2 / scale_2`
- `strategy_drawdown_level_3 / scale_3`
- `strategy_drawdown_reduce_only_level_3`

含义：

- 当账户权益相对历史高点回撤到不同区间时，系统自动缩小新目标仓位
- 到最严重一档时，可以只允许减仓，不允许再加新风险

作用：

- 防止系统在连续失灵阶段仍按原速度继续亏
- 把“信号判断”和“账户活下来”分开处理

## 4. 当前在 wzy 中的实现形态

### 4.1 新增模块

- `src/btc_regime/v71_live.py`
  - V7.1-live 主策略
- `src/btc_regime/live_risk.py`
  - 账户级回撤 governor
- `scripts/compare_three_branches_same_basis.py`
  - 三路线同口径比较脚本

### 4.2 新增配置

- `configs/v71_reference_params.json`
  - 参考版 `xuyujian V7.1`
- `configs/v71_live_params.json`
  - `wzy` 的保守实盘版
- `configs/v71_live_execution.json`
  - maker + governor 的统一执行配置

### 4.3 新增测试

- `tests/test_live_risk.py`
- `tests/test_v71_live.py`

## 5. 同口径比较的定义

三路线比较脚本当前采用同一套口径：

- 同一份 4h 市场数据
- 同一份 1m 执行数据
- 同一份资金费数据
- 同一套分钟级执行引擎
- 同一套 maker / fee / impact / liquidation 假设
- 同一套 governor 执行约束

当前比较对象是：

- `main_v43`
- `xuyujian_v71_ref`
- `wzy_v71_live`

## 6. 下一步建议

最值得继续做的不是马上扩很多参数，而是三件事：

1. 用同口径脚本先跑出第一版比较报告
2. 看 `wzy_v71_live` 相对 `xuyujian_v71_ref` 的回撤收缩幅度和值不值
3. 再决定是否继续加强做空，还是优先优化退出与 governor 阈值
