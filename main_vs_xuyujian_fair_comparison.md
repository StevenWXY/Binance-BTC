# main 与 xuyujian 公平对比、交易流程解释与实盘设计建议

## 1. 文档目的

这份文档回答三个问题：

1. 如何对 `main` 和 `xuyujian` 两条研发路线做一次尽量公平的比较。
2. 如何用自然语言解释两条路线的交易流程，也就是“什么时候买、什么时候卖”。
3. 如果要以这两个分支为基础设计适合实盘测试的策略，应该怎样组合、约束和优化。

本文的结论不是“哪个分支绝对更强”，而是帮助后续做实盘测试设计时，分清：

- 哪条线更适合当稳定基线
- 哪条线更适合当进攻性候选
- 哪些结果可以直接拿来参考
- 哪些结果还需要继续验证

## 2. 公平比较口径

### 2.1 什么叫“公平”

这两个分支并不是在做完全同一件事：

- `main` 主要在 V4 家族上做执行层、风险层、资本分配层和压力测试增强。
- `xuyujian` 主要在 V4/V5 之上继续探索 V6、V7、V7.1 这种更复杂的策略结构。

所以不能简单用“谁最终权益更高”来硬比。

更合理的公平比较方式，是分成三层：

1. **共同基线层**：比较两边都承认的 V4/V4.1/V4.2 基线逻辑。
2. **增量改进层**：比较各自新增内容解决的是什么问题。
3. **实盘适配层**：比较谁更接近真实交易、谁的验证更完整、谁更适合先上线测试。

### 2.2 本文采用的证据

本文主要依据以下材料：

- `main` 的 `README.md`
- `main/src/btc_regime/v42.py`
- `main/src/btc_regime/v43.py`
- `main/tests/test_v42.py`
- `main/tests/test_v43.py`
- `main/tests/test_stress.py`
- `main/reports/v4_family_validation_2026_09_02/report.json`
- `main/reports/v4_2_rebate30/report.json`
- `main/reports/v4_3_rebate30/report.json`
- `xuyujian/README.md`
- `xuyujian/AGENTS.md`
- `xuyujian/src/btc_regime/v6.py`
- `xuyujian/src/btc_regime/v7.py`
- `xuyujian/tests/test_v6.py`
- `xuyujian/tests/test_v7.py`
- `xuyujian` 分支中关于 V7.1 的统一复测说明和参数文件

### 2.3 需要先声明的公平性限制

有三点必须先说明：

1. `main` 的强项是 V4 家族和执行层增强，不是 V7。
2. `xuyujian` 的亮点是 V7/V7.1，但这套结果有一部分来自朋友提供的 `data.zip` 统一复测口径。
3. 因此，**V4 家族可以直接公平比较；V4.3 和 V7.1 只能做“目标函数不同”的比较，不能当同一实验条件下的直接赛跑。**

## 3. 两条分支各自的研发主线

### 3.1 main：在 V4 家族上做工程化增强

`main` 的思路可以概括为：

> 先保留 V4 这条已经能解释、能复现的方向策略，再逐步把执行、风险、资金分配和压力测试补齐。

它的演进顺序基本是：

- V4：稳健长多趋势 + 震荡反弹基线
- V4.1：冻结结构，只做局部参数优化
- V4.2：降低方向仓风险预算，并加入因果型 neutral sleeve
- V4.3：加入 maker 执行、ATR 止损/止盈、快速下跌保护
- stress-test / validate_v4_family：把极端行情验证补上

这条线的核心不是“换一种更复杂的交易哲学”，而是把已有策略变得更像真实交易系统。

### 3.2 xuyujian：继续做策略形态创新

`xuyujian` 的思路可以概括为：

> 不只优化 V4，而是继续往 V6、V7、V7.1 这种更复杂的状态机和风险控制结构推进。

它的演进顺序大致是：

- V4：稳健长多趋势/反弹混合
- V5：谨慎做空对照
- V6：多周期置信度驱动的风险控制
- V7：互斥三态 `range / trend_up / trend_down`
- V7.1：在 V7 基础上做回撤控制和执行层降仓

这条线更像“继续追求更强 alpha 的策略实验分支”。

## 4. 公平比较结论

## 4.1 共同基线层：V4 家族谁更稳

如果只看双方共同认可的 V4 家族，`main` 的研究闭环更完整。

原因不是它收益一定最高，而是它补齐了这几件事：

- V4.1 的局部参数优化
- V4.2 的方向仓缩放 + neutral sleeve
- V4.3 的 maker 执行和保护退出
- 压力测试与统一验证

从 `main/reports/v4_family_validation_2026_09_02/report.json` 可以看到，`main` 对 V4/V4.1/V4.2 做了同口径验证：

| 策略 | 层级 | Final Equity | Sharpe | Max DD |
| --- | --- | ---: | ---: | ---: |
| V4 | 1m direction | 639,099 | 1.618 | -30.78% |
| V4.1 | 1m direction | 671,638 | 1.635 | -30.83% |
| V4.2 | 1m direction | 287,303 | 1.653 | -23.68% |
| V4.2 | 1m combined neutral | 313,354 | 1.691 | -23.58% |

这个结果非常清楚地说明了三件事：

- `V4.1` 是收益略高、回撤几乎不变的局部优化版。
- `V4.2` 明显是“主动牺牲收益换回撤”的版本。
- `V4.2 + neutral sleeve` 进一步改善了风险收益比，但本质仍是一个更保守的组合。

如果目标是“做实盘测试的第一版基线”，共同基线层我会优先选 `main` 的 `V4.1` 或 `V4.2`，而不是直接选更复杂的新结构。

## 4.2 增量改进层：main 在补真实交易，xuyujian 在补策略表达力

### main 增量的价值

`main` 的增量重点有两个：

1. **V4.2 neutral sleeve**
2. **V4.3 execution + protection**

#### V4.2 的价值

`v42.py` 不是改信号，而是改组合层：

- 方向仓只占 75%
- 预留 free margin
- 剩余一部分做资金费中性覆盖
- 发生较大回撤或保证金吃紧时，会回收 neutral allocation

这很像实盘里“把高风险方向暴露和低风险 carry 分开管理”的组合思路。

#### V4.3 的价值

`v43.py` 不是重新定义趋势，而是把执行和退出做得更接近真实：

- 常规调仓尽量 maker 成交
- 止损、止盈、保护退出仍然用 taker
- 完成的 4h K 线出现快速下跌信号后，下一边界直接降目标仓位

在 `v4_3_rebate30/report.json` 里，V4.3 的代表性结果是：

- Final Equity: 453,891
- Sharpe: 1.643
- Max DD: -24.19%
- maker_fill_ratio: 58.24%
- maker_fee_saved_vs_taker: 23,785

这说明 `main` 的新增价值，不只是“策略收益更高”，而是：

- 成交成本更低
- 风险退出更明确
- 结果更容易映射到真实撮合约束

### xuyujian 增量的价值

`xuyujian` 的增量重点有两个：

1. **V6 的多周期置信度风险控制**
2. **V7/V7.1 的互斥三态状态机**

#### V6 的价值

`v6.py` 的思路是：不要只看一个趋势判断，而是根据置信度决定开试探仓还是满仓。

它会综合：

- 4h EMA / ADX / DI
- 日线趋势偏置
- 动量
- 资金费惩罚
- 价格回撤缩仓
- 显式 stop / take-profit / trailing stop

这套结构的优点是：比 V4 更像“仓位有层级的主动管理系统”。

#### V7/V7.1 的价值

`v7.py` 的核心是把市场先划成三种互斥状态：

- `range`
- `trend_up`
- `trend_down`

然后再分别决定：

- 震荡时是否低位做多
- 上涨趋势时是否做趋势多
- 下跌趋势时是否做趋势空
- 行情过快时是否减仓

这比 V4 的“趋势/非趋势 + 反弹”结构更复杂，但表达力也更强。

V7.1 再往前一步，加了：

- breakout buffer
- 更稳一点的 trailing stop
- 执行层权益回撤缩仓

从策略研究角度，它明显比 `main` 更进攻、更有野心。

## 4.3 验证强度：main 更完整，xuyujian 更偏前沿实验

如果按“谁更像一个可以上生产前做试运行的系统”来评分，我会这样看：

### main 更强的地方

- 有 `test_v42.py`，专门测 neutral sleeve 因果性和 allocation hysteresis
- 有 `test_v43.py`，专门测快速下跌保护和 maker 触价成交
- 有 `test_stress.py`，专门测合成极端路径、强平与保护字段
- 有统一的 `validate_v4_family.py`

这说明 `main` 不只是“做出一个新版本”，而是在补研究工程需要的验证闭环。

### xuyujian 更强的地方

- `test_v7.py` 对状态机结构覆盖得比较细
- 会测 trend_up / trend_down / range 的互斥关系
- 会测 rapid mode、range percentile、trailing stop、做空分支

这说明 `xuyujian` 对“新策略结构是否逻辑自洽”是认真验证过的。

### 但两者的差异是

- `main` 更偏“执行层和风险层的系统级验证”
- `xuyujian` 更偏“信号结构与状态机正确性验证”

如果你马上要做实盘测试，前者更重要。

## 4.4 最终评价

如果只给一句话：

- `main` 更适合当 **实盘前基线主干**
- `xuyujian` 更适合当 **高收益候选和影子策略**

如果再展开一点：

### 我对 main 的评价

- 优点：稳、解释清楚、执行与风险假设显式、验证更完整
- 缺点：策略创新没那么激进，收益上限未必最高

### 我对 xuyujian 的评价

- 优点：策略表达力强，状态机更丰富，有机会做出更高收益版本
- 缺点：复杂度高，过拟合风险更大，离“直接实盘试运行”还差一层收敛验证

## 5. 用自然语言解释两条路线的交易流程

下面不讲公式，直接用举例说明。

## 5.1 main 的交易流程：先判断大方向，再按规则进出

可以把 `main` 的 V4/V4.1 理解成一个比较克制的长多系统。

### 买入例子 1：趋势多头

假设最近一段时间 BTC 从 95,000 一路涨到 101,000：

- 4 小时均线开始明显多头排列
- ADX 提示“这不是震荡，是有趋势”
- +DI 也强于 -DI

这时系统会说：

“现在不是抄底反弹环境，而是上涨趋势环境。那我就按趋势仓位去买。”

它不会一下梭哈，而是先算：

- 现在波动大不大
- 当前资金费是不是太拥挤
- 下行风险占比是不是偏高

如果这些风险条件都还行，就在下一根 4 小时边界开始建立多头。

### 卖出例子 1：趋势结束

同样这笔多头持有了一段时间后，市场开始转弱：

- ADX 回落
- 趋势强度不足
- 风险层变差

那系统会把目标仓位调低，甚至直接回到空仓。

如果是 `main` 的 `V4.3`，它还会更进一步：

- 如果出现完成 4h K 线的快速下跌
- 或者 ATR 止损、止盈、追踪止损被触发

那么会更快退出，不再等普通趋势规则慢慢变坏。

### 买入例子 2：震荡里的超跌反弹

假设 BTC 没有趋势，只是在区间里来回波动，突然连续下跌到布林带下轨附近，RSI 也很低。

这时系统会说：

“现在不像趋势上涨，更像震荡里的短期超跌。那我可以轻仓去接一个反弹。”

所以它会买，但仓位比趋势单更小，而且会更早止盈。

### 卖出例子 2：反弹结束

如果价格回到布林中轨，或者 RSI 回升，或者反弹失败继续下跌触发止损，系统就会卖出。

你可以把 `main` 理解成：

> 平时尽量顺着趋势做多，偶尔在明显超跌时做小仓位反弹；一旦市场不对，就优先撤退。

## 5.2 xuyujian 的交易流程：先分类市场，再决定做多、做空还是观望

`xuyujian` 的 V7/V7.1 更像一个先分场景再行动的系统。

它不是先问“要不要买”，而是先问：

“现在到底是震荡、上涨趋势，还是下跌趋势？”

### 买入例子 1：range 状态里的低位买入

假设 BTC 最近三周一直在一个箱体里来回震荡，没有明显趋势。

系统综合看完后，判断这是 `range` 状态。

然后它再看：

- 当前价格是不是接近最近一段时间的低分位
- RSI 是否偏弱
- 日线没有明显转空

如果这些都满足，它会说：

“这不是趋势追涨，而是箱体里偏低的位置，可以试着买一点等回归。”

这就像一个更规则化的“区间低吸”。

### 卖出例子 1：range 状态结束

如果后来价格回到区间高位，或者开始像突破，或者持有时间太久还没弹起来，系统就会退出。

也就是说，`range` 里的单子目标不是吃大波段，而是吃回归。

### 买入例子 2：trend_up 状态里的趋势做多

假设 BTC 先震荡，后来放量突破，均线、Aroon、ADX、效率比这些指标都开始支持上涨趋势。

这时系统会把市场从 `range` 切成 `trend_up`。

一旦切换成功，系统会说：

“现在不是低吸逻辑，而是趋势逻辑。我要换成趋势多头仓位。”

如果行情只是正常上涨，就按 normal 模式持仓。
如果涨得特别快，系统还会把它标成 rapid 模式，然后根据是否出现减速，适当缩仓。

### 卖出例子 2：上涨趋势衰竭

如果上涨趋势走到后面开始衰竭：

- trailing stop 被打到
- 日线状态不再支持
- 4 小时趋势明显减弱

那系统会离开 `trend_up`，回到 `range`，甚至在满足条件时转成 `trend_down`。

### 做空例子：trend_down 状态

这就是 `xuyujian` 和 `main` 最不一样的地方。

假设 BTC 明显跌破区间、均线转空、趋势确认持续成立：

系统会把它认成 `trend_down`，然后不是空仓观望，而是会建立一个受限的空头。

通俗讲，V7/V7.1 的想法是：

> 市场下跌时，不一定只能等它跌完，我也可以用一部分风险预算去吃下跌趋势。

这就是它收益上限更高、但同时复杂度和风险也更高的原因。

## 6. 如果要设计适合实盘测试的策略，我建议怎么做

我的建议不是“直接从两条线里选一个冠军”，而是做一个 **分层实盘测试框架**。

## 6.1 实盘测试不要直接上最强收益版

如果目标是实盘测试，优先级应该是：

1. 因果性是否明确
2. 执行假设是否接近真实
3. 回撤是否在你能接受的范围内
4. 信号是否稳定
5. 最后才是收益上限

按这个标准，直接拿 `V7.1` 上实盘并不理想。

更稳的做法是把两条线拆成：

- **主策略**：负责稳定试运行
- **影子策略**：负责并行观察，先不真实下单或只小仓位下单

## 6.2 推荐的实盘原型结构

### 方案 A：最稳妥

- 主策略：`main` 的 `V4.1`
- 执行层：吸收 `main` 的 `V4.3` 保护退出思想
- 影子策略：`xuyujian` 的 `V7.1`

这套组合的意思是：

- 先用 V4.1 做稳定方向基线
- 用 V4.3 的快速下跌保护、止损和 maker/taker 区分补执行
- 同时把 V7.1 作为影子策略跑同市场、同时间、同手续费条件

优点是风险最低，最适合第一阶段实盘测试。

### 方案 B：中等进攻

- 主策略：`main` 的 `V4.1 + V4.3 protection`
- 卫星仓：`xuyujian` 的 `V7.1`，但只给 10% 到 20% 风险预算

意思是：

- 大部分资金仍由稳健的长多基线控制
- 少量风险预算给 V7.1 的趋势做空和状态机增强

这适合你已经接受“实盘测试本身就是试错”，并且愿意换取更高收益弹性的时候。

### 方案 C：偏研究，不建议第一阶段上实盘

- 直接以 `V7.1` 为主策略
- 再叠执行层回撤控制

这个方案的风险在于：

- 复杂度太高
- 做空分支会带来更多行为变化
- 一旦实盘表现偏离回测，很难快速定位是哪个模块出问题

所以我不建议把它作为第一版实盘测试方案。

## 6.3 我建议的优化方向

如果你的目标是“做适合实盘测试的策略”，优化方向要从“拉高全样本收益”改成“降低实盘偏差风险”。

我建议按下面这几个方向优化：

### 方向 1：先优化退出，不先优化入场

优先级建议是：

1. 快速下跌保护
2. trailing stop
3. 分层减仓
4. 再看入场阈值

原因很简单：实盘里最痛的通常不是“少赚了一段”，而是“该撤没撤”。

### 方向 2：把做空先降级为卫星模块

`xuyujian` 的做空是有价值的，但不建议一开始就作为主收益来源。

更好的方式是：

- 只在非常明确的 `trend_down` 状态开空
- short_scale 先低一点
- 做空只占组合风险预算的一小部分

也就是说，先把它当“增强模块”，不是当“主引擎”。

### 方向 3：优化目标改成多目标，不再只看最终权益

以后筛参数，不建议再只盯：

- final_equity
- CAGR

更应该同时约束：

- holdout Sharpe
- holdout max drawdown
- trade_count / turnover
- fees_paid
- funding_paid
- maker fill ratio
- worst stress path drawdown

一句话就是：

> 参数好，不等于赚得最多；参数好，应该是实盘偏差下还活得好。

### 方向 4：实盘测试前加一层“状态一致性监控”

不管主策略选哪条线，实盘前都建议做三类实时监控：

1. **信号一致性**
   - 本地回测同一时间点应得到同一目标仓位
2. **成交一致性**
   - 真实成交价和模拟成交价偏差是否扩大
3. **风险一致性**
   - 实时杠杆、保证金缓冲、资金费暴露是否超出回测分位区间

这一步比继续抠 0.01 的 Sharpe 更重要。

## 6.4 推荐的实盘推进节奏

我建议按四阶段走：

### 阶段 1：影子盘

- `V4.1 + V4.3 protection` 跑主策略影子盘
- `V7.1` 同时跑影子盘
- 不下真实单，只记录信号、成交模拟、实时回撤

### 阶段 2：小仓试单

- 主策略先上 5% 到 10% 风险预算
- V7.1 只做旁路记录，或者更小试单

### 阶段 3：双策略对照

- 评估 4 到 8 周
- 比较主策略与影子策略在真实市场上的：
  - 信号稳定性
  - 滑点偏差
  - 成本偏差
  - 最大单周回撤

### 阶段 4：再决定是否把 V7.1 升级成卫星仓

只有在下面条件同时满足时，才考虑把 V7.1 提升到真实资金层：

- holdout 和实时窗口都没有明显崩坏
- 做空阶段没有显著放大回撤
- 真实成交偏差没有毁掉回测 edge

## 7. 最终建议

如果你让我给一个明确建议，我会这样选：

### 第一版实盘测试主线

- **主策略**：`main` 的 `V4.1`
- **执行与退出**：吸收 `main` 的 `V4.3` 保护逻辑
- **风险预算**：保持长多为主，不急着引入大比例做空

### 第一版实盘测试的研究副线

- **影子策略**：`xuyujian` 的 `V7.1`
- **目的**：验证三态状态机和对称做空，在真实市场中到底有没有稳定增益

### 为什么这样选

因为对实盘测试来说，最重要的不是“历史上谁最猛”，而是：

- 谁更容易解释
- 谁更容易定位问题
- 谁更能控制首轮试错成本

这三点上，`main` 更适合作为主线；而在寻找更高收益上限这件事上，`xuyujian` 更值得继续做副线研究。

## 8. 一句话总结

`main` 更像可以拿来做第一阶段实盘测试的稳健主干，`xuyujian` 更像值得继续追踪和小仓验证的高弹性候选；最优做法不是二选一，而是“main 做主、x 做影子，再用真实交易数据决定是否升级”。 
